#include <stdio.h>

int main() {

    int a = 2;    

    // == igual
    // > maior
    // < menor
    // >= maior ou igual
    // <= menor ou igual
    // != diferente

    // verifica se a variável "a" armazena um valor igual a "2"
    if(a == 2) {
        // trecho que será executado caso a condição seja verdadeira
        printf("A variável (a) armazena um valor par");
    }

    printf("\n");
    return 0;
}
